org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.jvmargs=-Xmx6G
# Mod Properties
archives_base_name=fluidphysics
mod_version=0.0.1-SNAPSHOT
maven_group=de.lolhens
mod_id=fluidphysics
mod_author=LolHens

# mcdp (MC Dependency Provider) version. Declared at root level so the `plugins {}` block
# in build-1.21.1.gradle can resolve `id 'de.lhns.mcdp' version "$project.mcdp_version"`.
# The subproject gradle.properties files (fabric-1.21.1 / neoforge-1.21.1) reference the
# same property for the runtime `de.lhns.mcdp:mcdp:${mcdp_version}` dependency.
mcdp_version=0.1.1
