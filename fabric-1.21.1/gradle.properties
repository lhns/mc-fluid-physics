# Minecraft / Fabric
minecraft_version=1.21.1
loader_version=0.16.5
fabric_version=0.116.11+1.21.1

# Mappings
parchment_minecraft=1.21.1
parchment_version=2024.11.17

# Scala
scala3_version=3.5.2

# mcdp_version is declared in the root gradle.properties (so the plugins{} block in
# build-1.21.1.gradle can read it). The runtime artifact `de.lhns.mcdp:mcdp:${mcdp_version}`
# is the unified jar (ADR-0016) bundling both loader adapters; both plugin and artifact are
# resolved from the Sonatype Central Portal snapshots repo (configured in
# settings-1.21.1.gradle and the per-subproject build.gradle).

# cats (declared as mcdepImplementation; served by mcdp's per-mod loader at runtime)
cats_version=2.13.0

# circe
circe_version=0.14.10
circe_extras_version=0.14.5-RC1
circe_config_version=0.10.2
