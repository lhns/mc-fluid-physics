# Minecraft / Fabric
minecraft_version=1.21.1
loader_version=0.16.5
fabric_version=0.116.11+1.21.1

# Mappings
parchment_minecraft=1.21.1
parchment_version=2024.11.17

# Scala
scala3_version=3.5.2

# mcdp (MC Dependency Provider) — Scala language provider + per-mod URLClassLoader for Maven
# dep loading. Replaces Scalable Cat's Force on both Fabric and NeoForge. Source repo:
# C:/Users/pierr/Documents/git/mc-scala, composite-included via settings-1.21.1.gradle. Runtime
# artifact `de.lhns.mcdp:mcdp-fabric:${mcdp_version}` is resolved from mavenLocal — before
# building, run:
#   ../mc-scala/gradlew :fabric:publishToMavenLocal
mcdp_version=0.1.0-SNAPSHOT

# cats (declared as mcdepImplementation; served by mcdp's per-mod loader at runtime)
cats_version=2.13.0

# circe
circe_version=0.14.10
circe_extras_version=0.14.5-RC1
circe_config_version=0.10.2
